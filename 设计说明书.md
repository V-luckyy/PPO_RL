# PPO 双足机器人行走 - 设计说明书

## 1. 软件概述与技术选型
本项目从零实现了一套基于 **Proximal Policy Optimization (PPO)** 深度强化学习算法的双足机器人行走仿真与可视化训练系统。
系统完全摒弃了成熟的第三方物理刚体引擎（如 MuJoCo 或 PyBullet），采用自主构建的纯数学连杆差分极简物理环境，能够有效演示强化学习在非稳态强随机动力学系统中如何对抗重力、建立复杂协调步态的原理。

核心技术架构选型：
- **深度强化学习算法核心**：PyTorch
- **强化学习环境封装**：OpenAI Gym API 规范
- **图形用户界面体系**：Tkinter 
- **统计学绘图集成**：Matplotlib (嵌于 Tkinter)
- **实时动画回放引擎**：Pygame

## 2. 核心算法设计（PPO）

本项目底层使用的是 Actor-Critic 架构的 PPO 连续动作版本：
1. **策略网络（Actor）**：接收机器人的实时环境状态作为输入，其两层全连接层提取特征后，输出高斯分布的**均值**（各个关节力矩的大小）与**标准差**（用于随机探索）。通过向分布中采样获取具体的扭矩和推力命令输出到环境中。
2. **价值网络（Critic）**：评估当前姿态距离“成功行走”或“即将跌倒”的相对优劣（即估算其期望累积回报）。
3. **优势函数计算**：利用广义优势估计（GAE），以时间步衰减法平滑估计了未来奖励对当前动作的积极反馈，将蒙特卡洛回报的高方差和 TD 误差的高偏差进行了折衷。
4. **PPO Clip 更新机制**：由于连续步长梯度极易让策略崩溃，我们在模型更新反向传播时实施了裁剪惩罚机制：`surr2 = torch.clamp(ratio, 1.0 - epsilon, 1.0 + epsilon) * advantages`。这约束了更新幅度，保障了机器人每次跌倒后模型经验吸收的稳定收敛。

## 3. 动力学环境与模型设计 (15 维系统)

为了逼真地反映双足生物的步态，该项目搭建了独立的双侧（左/右腿）受力计算骨架：

### 3.1 状态空间（15 维连续观测）
模型在每个时间戳 `dt=0.02` 可获取以下传感器反馈：
- **躯干位置与速度**：`[Torso_X, Dot_Torso_X, Torso_Z, Dot_Torso_Z]`
- **左侧关节位置与速度**：`[Theta_Hip_L, Dot_Hip_L, Theta_Knee_L, Dot_Knee_L]`
- **右侧关节位置与速度**：`[Theta_Hip_R, Dot_Hip_R, Theta_Knee_R, Dot_Knee_R]`
- **双足对地法向接触力**：`[Fz_L, Fz_R]` (脚后跟高度探针传感器)
- **辅助周期步态指引相位波**：`[Cos(step)]`

### 3.2 动作空间（6 维连续执行）
策略网络输出映射至以下执行机构：
- 左侧及右侧的**髋关节角速度驱动力矩**
- 左侧及右侧的**膝关节角速度驱动力矩**
- 足底在着地时触发的**整体向下蹬踹力摆幅推力 (`f_swing`)**

### 3.3 物理引擎实现逻辑
1. **重力引入**：施加向下的全局加速度 $G=9.81$。
2. **足底着陆拦截**：网络计算躯干高度与腿长三角函数的绝对差，一旦 `ankle_height < 0.05`，视为发生地面碰撞。
3. **法向支撑力解离**：大地对触发碰撞的那只脚产生抵消跌倒的结构支撑反力，辅以大腿主动网络推力 (`f_swing`)，运用三角函数投影至抵抗 Z 轴跌倒与提供 X 轴前进的分力。

## 4. 奖励设计 (Reward Shaping)
强健步态的养成极其依赖对环境的正负反馈，本项目构建了综合评价奖励机制，其组成分别为：
- **生存与平衡奖励** (Balance)：越接近 $Z=0.8$ 的设定高度，奖励最大。
- **前进进度奖励** (Forward)：利用质心的 X 轴正向速度给予增益。
- **分离步态强制** (Gait)：强烈惩罚两腿粘合在一起的双脚跳动作，通过 $0.5 \times |\theta_{hip\_L} - \theta_{hip\_R}|$ 的插值强制要求两腿在空间交错。
- **安全惩罚** (Safety)：阻止为了加速推进而触发极端力量砸向地面带来的硬直或自毁。

## 5. 项目模块结构划分
- **`configs/default.py`**：定义所有环境及模型超参数常数。
- **`envs/bipedal_env.py`**：Gym 规范下物理受力计算、极坐标解析、离地高度探测与环境更新循环核心。
- **`models/networks.py`**：PyTorch 底层模型结构定义。
- **`models/ppo.py`**：GAE 取样与 PPO 主干网络训练调度。
- **`utils/robot_replay.py`**：使用 Pygame 开发的高帧率 2D 着色动画绘制外挂工具。
- **`utils/visualization.py`**：收集与绘制基于周期去噪去重复阶梯的平滑训练曲线。
- **`gui_app.py`**：界面与多线程的统管层入口。
