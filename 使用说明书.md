# PPO 双足机器人行走 - 使用说明书

本说明书将引导您从安装依赖完成到最终评估检阅模型结果的全套标准操作流。

## 一、 环境依赖与软件安装

1. **Python 环境搭建**：
   确保您的系统已安装了 Python 环境，以及 `pip` 包安装依赖程序。
   
2. **下载与解压**：
   将 PPO 双足机器人项目代码存放至您指定的目录。

3. **依赖包安装**：
   在命令行终端中，进入软件根文件夹内，并运行以下脚本来获得计算引擎以及画图组件依赖的支持：
   ```bash
   pip install -r requirements.txt
   ```
   *（必装组件含 `torch`, `pygame`, `matplotlib`, `numpy`, 及内置 `tkinter` 等）*

## 二、 运行主程序

在项目根目录下打开终端，输入以启动系统图形前端：
```bash
python gui_app.py
```
若正常执行，桌面将跳出宽幅界面的后台统调窗口。

![占位图片：GUI 启动初始化首屏截图]

界面大体分为左侧【控制按键和参数调配区】与右侧【曲线监控区】。

---

## 三、 使用流程：模型训练 (Training)

### 步骤1：设定模型参数
训练面板会预设一套被认定可以生效行走的经验参数。如需科研测试可自行微调：
- **学习率**：PPO 中策略神经网络更新跨度的剧烈程度。
- **总训练步数**：机器人在环境里的活动总长度，推荐至少跑 `20000` 步才能看出它脱离随机乱蒙并开始学会站立的意图。
- **Episode 最大步数**：如果能坚持1000步不摔倒才算一次彻底完成的优秀循环。

### 步骤2：启动并监听进程
确认参数无误，点击【**开始训练**】。
- 软件状态栏将显示 “训练中...” 的运行词条。
- 此时右侧的 Matplotlib 画板界面开始每隔一段时间向外吐出线段。红色线是网络的错误率判断估算（应逐渐平稳下沉），绿线代表机器人获得的有效行走存活奖励点（应呈现上升或维持顶峰阶层）。
- 随时可点击【**停止训练**】中断当前循环，已达成的网络会自动保存至根目录下的 `ppo_bipedal_model.pth` 文件以备检阅。

![占位图片：训练进行中，界面右侧产生走高绿线奖励与跌落红线损耗趋势的截图]

---

## 四、 使用流程：环境评估 (Evaluation / Playback)

当你完成训练或者手中拥有旧的模型断点存档时，可以转入【评估面板区】观察控制网络的疗效。

### 测试步骤

1. **加载模型权重**
   在左侧参数栏下半区，点击【**加载模型并评估**】。系统将会尝试激活被储存在本地盘符网络大脑。此时这套“刚学成的大脑”会立刻在无声无息的环境里虚拟推演一次从起立到失败的全动作流，产生出一段虚拟状态历史。如果在这一步提示失败，请先完整等待一次【开始训练】结束并落盘。

2. **检阅双足折叠极限（静态透视）**
   点击【**查看机器人姿态**】。将立刻弹出次级图片预览窗口。
   在此窗口中：
   - 黑色的粗线是主躯干。
   - 前置层拥有深蓝（大腿）、深绿（小腿）和红色代表了**左腿关节态**。
   - 后置层拥有淡蓝（大腿）、浅绿（小腿）和肉色代表了**右腿关节态**。
   可以清楚地根据图片与数据看到机器人在力学解算下的伸缩俯卧程度。

  ![占位图片：静态双足机器骨架 2D 切片弹窗，清楚显示多色腿交叠]

3. **进入全景虚拟放映（动态引擎回放）**
   确认完毕静态骨架没有断裂或死锁后，切回主操作台，点击最后的一枚按钮【**实时仿真回放**】。
   系统将直接调取您的显卡挂载 Pygame 高刷动画黑窗。
   
  - 在黑窗内，你能直观地见到机器人双腿从初始姿态往下扎或者迈步抗争物理法则。
  - 同时地面会有灰色网格向后拖拽移动，以协助您使用肉眼捕捉躯干的位移并判断它是向右正常行走、倒退或者是抽搐跌倒。
  - 按下键盘上的 `Esc` 键，或右上角退出按钮立即结束放映模式并安全返回 PPO 控制中枢。
  
  ![占位图片：黑底带有灰网格底线的动态窗口抓拍机器人腾空行走动作]
